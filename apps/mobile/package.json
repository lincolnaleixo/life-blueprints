{
  "name": "@matrix/mobile",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "cap sync",
    "android": "cap open android",
    "ios": "cap open ios",
    "build": "bun run --cwd ../web build:native && cap sync",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@capacitor/core": "catalog:"
  },
  "devDependencies": {
    "@capacitor/android": "catalog:",
    "@capacitor/cli": "catalog:",
    "@capacitor/ios": "catalog:",
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}
