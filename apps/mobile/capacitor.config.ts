import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.matrix.template',
  appName: 'Matrix Template',
  webDir: '../web/dist/client',
}

export default config
